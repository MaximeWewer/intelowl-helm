name: Release Helm Chart

on:
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Convert repository owner to lowercase
        id: lowercase
        run: |
          OWNER_LOWER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "owner=${OWNER_LOWER}" >> $GITHUB_OUTPUT

      - name: Compute chart version with date suffix
        id: chart
        run: |
          BASE=$(grep '^version:' chart/Chart.yaml | awk '{print $2}')
          DATE=$(date -u +'%d-%m-%Y')
          VERSION="${BASE}-${DATE}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Login to GitHub Container Registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Update chart dependencies
        run: |
          helm dependency update chart/

      - name: Package Helm chart
        run: |
          helm package chart/ --version ${{ steps.chart.outputs.version }}

      - name: Push Helm chart to GHCR
        run: |
          helm push intelowl-${{ steps.chart.outputs.version }}.tgz oci://ghcr.io/${{ steps.lowercase.outputs.owner }}/charts
